/*
 * Copy this file to `secrets.h` and fill in your values.
 * `secrets.h` is git-ignored.
 */
#ifndef SECRETS_H_
#define SECRETS_H_

#define WIFI_SSID  "your-wifi-ssid"
#define WIFI_PSK   "your-wifi-password"

/* PC server (the host running pc_server/server.py) */
#define PC_SERVER_HOST "192.168.1.100"
#define PC_SERVER_PORT 8080

#endif /* SECRETS_H_ */
