/*
 * secrets.h.example — copy to secrets.h and fill in your own values.
 *
 * NEVER commit the real secrets.h to git (already in .gitignore).
 *
 * Used by: launcher_full (AI Vision — qwen-vl-plus + CosyVoice TTS)
 */
#ifndef SECRETS_H
#define SECRETS_H

/* ── Wi-Fi ───────────────────────────────────────────── */
#define WIFI_SSID "your_wifi_ssid"
#define WIFI_PSK  "your_wifi_password"

/* ── DashScope (Aliyun bailian) API key (sk-xxxx) ────── */
/* Apply at https://dashscope.console.aliyun.com/                         */
/* Required model permissions:                                            */
/*   - qwen-vl-plus     (multimodal vision/text)                          */
/*   - CosyVoice (TTS)  (audio synthesis, e.g. cosyvoice-v2)              */
#define BAILIAN_API_KEY "sk-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

#endif /* SECRETS_H */
